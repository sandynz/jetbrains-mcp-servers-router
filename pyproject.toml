[project]
name = "jetbrains-mcp-router"
version = "0.1.0"
description = "Route MCP tool calls to the correct JetBrains IDE based on project path"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
dependencies = [
    "mcp>=1.0",
    "httpx>=0.27",
]
